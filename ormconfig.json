{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "postgres",
  "database": "graphql_conduit",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "logging": true,
  "migrations": ["dist/migration/*.js"],
  "cli": {
    "migrationsDir": "src/migration/"
  }
}